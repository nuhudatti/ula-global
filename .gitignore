node_modules/
.env
.env.example
.env.production.example 
web/dist/
*.log
.DS_Store
prisma/dev.db
prisma/*.db
prisma/*.db-journal
prisma/sqlite-export.json
prisma/migration-report.json

# Local uploads when Cloudinary is not configured
uploads/

# Dev email outbox (when SMTP not configured)
data/email-outbox/

# Dev off-site database backups (outside repo)
ula-backups/
