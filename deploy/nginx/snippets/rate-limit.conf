# Rate limiting zones — include once in http {} context (see install script).
# limit_req_zone $binary_remote_addr zone=ula_auth:10m rate=10r/m;
# limit_req_zone $binary_remote_addr zone=ula_api:10m rate=120r/m;
