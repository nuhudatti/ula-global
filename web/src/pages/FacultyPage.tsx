import { FacultyWorkspace } from './faculty/FacultyWorkspace';

/** Faculty workspace — FACULTY_ADMIN only (department governance, not institutional admin). */
export function FacultyPage() {
  return <FacultyWorkspace />;
}
